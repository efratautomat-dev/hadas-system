# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-demo
dist-ssr
*.local
.env
.env.*
!.env.example
# Committed on purpose — the standalone demo build config holds no secret, and every
# machine that builds the demo needs it. See docs/08-DEMO-DEPLOYMENT.md.
!.env.demo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
# Screenshot scratch from an E2E run — captured output, not source.
/tmp-e2e/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Supabase CLI local state (link ref, pooler host, versions — env-specific, never commit)
supabase/.temp/

# Claude Code work log (local, per-machine)
.claude/work-log.md
