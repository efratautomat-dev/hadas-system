# ניקוי נתונים — מדידה מ-2026-08-06, והחלטה

> נמדד מול הפרודקשן (`jcwphkuwwuxvjibmvgdh`) בעזרת `scripts/data-health.mjs`.
> **בעלת העסק בחרה לדחות את הטיפול.** המסמך הזה שומר את התמונה כדי שלא נצטרך
> לגלות אותה מחדש. שום דבר לא שונה, לא נמחק ולא מוזג.

---

## ההחלטה

**אין צורך לבנות מסך ניקוי.** זו הייתה השאלה הפתוחה, וזו התשובה שהמדידה נתנה.

סך הכל **כ-25 פריטים** דורשים עין: 14 קבוצות חשבוניות כפולות, 2 קבוצות ספקים,
9 קבלות. זה היקף של אחר צהריים אחד עם הכלים שכבר קיימים במערכת — לא פרויקט.
אם המספרים יגדלו משמעותית בעתיד, שווה למדוד שוב לפני שמחליטים אחרת.

---

## התמונה כפי שנמדדה

היקף: **437 חשבוניות · 97 ספקים · 76 תשלומים · 8 כרטסות**

### קבלות שנכנסו כחשבוניות — הפריט הכספי הגדול
9 שורות, **₪42,410 מנופחים ביתרות הספקים**.

⚠️ **אחת מהן חשודה כזיהוי שגוי:** `חיה אורדמן בע״מ`, סכום **שלילי** של
₪15,546.92־. סכום שלילי הוא **זיכוי**, לא קבלה — המילה "קבלה" ככל הנראה מופיעה
בפירוט מסיבה אחרת. היא לבדה כמעט שליש מהסכום, כלומר היא משנה את התמונה.
**לפתוח את המסמך לפני כל פעולה.**

### חשבוניות כפולות
14 קבוצות, 15 שורות עודפות, **מתוכן רק 9 עדיין נספרות** ביתרה — ₪24,817.
השאר כבר מסומנות ולכן אינן מזיזות דבר. כלומר החלק הדחוף קטן מהמספר הראשוני.

### ספקים כפולים — 2, ושניהם אמיתיים
- **`סמארט סנט בע״מ` (SUP-038) ↔ `Smart Scent` (SUP-029)** — אותו ח.פ
  (`516727450`), אותו ספק בשתי שפות. 5 חשבוניות מפוצלות.
- **`בזק-ג'ן בע״מ` (SUP-007) ↔ `בזק-ג׳ן בע״מ` (SUP-069)** — **ההבדל היחיד הוא
  סוג הגרש**: אחד רגיל (`'`), אחד עברי (`׳`). בעין הם זהים לחלוטין, ולכן זה נוצר
  ולא נתפס. 8 חשבוניות מפוצלות.

שניהם ניתנים למיזוג מתוך מסך הספקים — הפעולה אטומית ולא מאבדת היסטוריה.

### כרטסות — נקי
**0 יתומות, 0 מסומנות `תואם` שכבר אינן תואמות.** זה סוגר מעצמו את האזהרה
ב-`docs/07-OPEN-ISSUES.md` פריט 3b — לא נמצאה אף כרטסת שהותאמה מול מספר ישן.

### חשבוניות חסרות
1 בלי סכום · 0 בלי ספק · 0 מסומנות כשגויות.
**31 עם סה"כ בלי פיצול מע"מ** — קדמו לפריסת השלמת הסכומים (2026-08-05) ומשלימות
את עצמן ברגע שפותחים אותן במסך. לא דורש טיפול יזום.

### תור ההתראות
23 פתוחות: 5 `invoice_old_date` · 4 `invoice_duplicate` · 4
`supplier_details_review` · 3 `supplier_incomplete` · 3 `invoice_ingest_failed` ·
2 `invoice_no_attachment` · 1 `invoice_low_confidence` · 1 `invoice_link_failed`.

**5 פתוחות מעל 30 יום.** התראה שאיש לא נגע בה חודש היא בדרך כלל רעש שמסתיר את
מה שכן דורש טיפול.

---

## סדר עבודה מוצע, כשחוזרים

1. **שני הספקים הכפולים** — מיזוג אחד לכל אחד. מהיר, ומשפיע ישירות על היתרות.
2. **9 הקבלות** — לפתוח את המסמך בכל אחת. **במיוחד את השלילית.** המלצה: לסמן
   `has_error` ולא למחוק — מוציא מהיתרה ומשאיר תיעוד ואת הקובץ בדרייב
   (אותו כלל "מוצג ולא נספר" של `spec/06-RULES.md §9`).
3. **9 הכפילויות שעדיין נספרות.** מסך חשבוניות ← מסנן "כפילויות".
4. **5 ההתראות הישנות.**

---

## איך למדוד מחדש

```
SUPABASE_URL=https://jcwphkuwwuxvjibmvgdh.supabase.co \
SUPABASE_ANON_KEY=<המפתח של PROD> \
HADAS_REPORT_EMAIL=<המייל> HADAS_REPORT_PASSWORD=<הסיסמה> \
node scripts/data-health.mjs
```

`--full` לרשימות מלאות · `--json` לפלט מכונה ·
`node scripts/receipt-audit.mjs` לקישור למסמך של כל קבלה.

**הכל קריאה בלבד.** הסקריפט מסרב למפתח service-role, מדפיס בראשו לאיזה פרויקט
התחבר, ונופל אם הכתובת והמפתח שייכים לפרויקטים שונים.

⚠️ קובץ ה-`.env` בריפו מצביע על **TEST**. חובה להעביר את פרטי הפרודקשן במפורש.
