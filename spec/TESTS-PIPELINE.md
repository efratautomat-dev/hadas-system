# בדיקות — פייפליין הסחורה

מקור: אפיון פרקים 6–7 וההכרעות שהתקבלו 25.08–03.09.2026 (`spec/STATUS.md`).
**כל בדיקה מנוסחת כך: מה עושים · מה אמור לקרות · איך נראית נפילה.**

איפה לבדוק: **בדמו** — `?demo=1` (מנהלת) ו‑`:5174` (בורר תפקיד).
הדמו לא נוגע בפרודקשן, וכל הפעולות בו אמיתיות בזיכרון.

---

## א · מודל שלוש הרגליים

> כל חלק פותח פייפליין בעצמו, ומחפש קודם אם קיים לו אחד.

**א1 · הזמנה פותחת פייפליין מיד**
פתחי הזמנה חדשה אצל ספק כלשהו.
→ היא מופיעה **גם בטאב הזמנות וגם ברשימת הסחורה**, בשלב `חשבונית ללא סחורה`.
✗ נפילה: היא מופיעה רק בטאב ההזמנות. אז הזמנה שוב אינה חלק מהשרשרת.

**א2 · תעודה שנקלטת ידנית פותחת פייפליין**
קליטת סחורה ← הקלדה. שמרי.
→ שורה חדשה ב`ממתין לחשבונית`.

**א3 · חשבונית שהגיעה ראשונה פותחת פייפליין**
פתחי חשבונית שאינה מקושרת ← שורת **סחורה** ← `ממתינה לסחורה`.
→ נוצר פייפליין ב`חשבונית ללא סחורה`, ובחשבונית מופיע `פתיחת הפייפליין`.
✗ נפילה: הכפתור נלחץ ולא קורה כלום. (היה באג כזה — היעדר רענון.)

**א4 · הרצועה מבחינה בין שני מצבי "ללא סחורה"**
השוו א1 מול א3.
→ בא1 קוביית **הזמנה** ירוקה וקוביית **חשבונית** לא. בא3 להפך.
✗ נפילה: שתיהן נראות זהה. אז `hasInvoice` לא מגיע לרצועה.

---

## ב · אין כפילויות

**ב1 · "הגיע" על הזמנה כשאין תעודה במייל**
→ השורה שההזמנה פתחה עוברת ל`ממתין לחשבונית`. **לא נוצרת שורה שנייה.**

**ב2 · "הגיע" כשכן הגיעה תעודה במייל**
צרי תעודה ידנית לספק, ואז לחצי "הגיע" על הזמנה של אותו ספק.
→ **המערכת שואלת** "כבר יש תעודה ממתינה" ומציעה לבחור.
→ בחירה ב"זו היא" ⇒ **שורה אחת**, של התעודה. השלד של ההזמנה נמחק.
→ "לא, זו סחורה אחרת" ⇒ שתי שורות, וזו בחירה מודעת.
✗ נפילה קריטית: המערכת מחליטה לבד. איחוד שקט מאבד משלוח.

---

## ג · הצמדה ואישור

**ג1 · הצעות ניתנות לפתיחה**
פתחי תעודה שממתינה לחשבונית.
→ רשימת מועמדות, **ולכל אחת כפתור צפייה במסמך**.
✗ נפילה: אפשר לבחור בלי לראות — כלומר לבחור לפי מספר בלבד.

**ג2 · הצמדה מקדמת את הרצועה**
→ השלב עובר ל`ממתין לאישור`.

**ג3 · אישור אחד סוגר הכל — החשבונית המרכזת**
הצמידי **שלוש** תעודות לאותה חשבונית ואשרי פעם אחת.
→ שלושתן ל`בכרטסת`, והסכום נכנס ליתרה **פעם אחת בלבד**.
✗ נפילה חמורה: היתרה גדלה פי שלוש.

**ג4 · העובדת יכולה לאשר**
באותה זרימה, בתפקיד עובדת.
→ האישור עובד. **הסכומים ריקים אצלה**, וההשוואה היא בין המסמכים.
✗ נפילה: מופיע סכום. זו דליפה.

**ג5 · החלפת חשבונית**
→ הכפתור אומר `החלפת חשבונית`, והשלב חוזר ל`ממתין לחשבונית`.

---

## ד · פירוק

**ד1 · פירוק אינו מוחק**
פרקי פייפליין עם חשבונית מוצמדת.
→ **החשבונית והתעודה נשארות** ברשימות שלהן, בלי קשר ביניהן.
✗ נפילה חמורה: מסמך נעלם.

**ד2 · שלד נמחק**
פרקי פייפליין שנפתח ע"י חשבונית ולא נשא סחורה.
→ השורה נעלמת. לא נשאר "משלוח" שלא היה.

**ד3 · לעובדת אין פירוק**
→ הכפתור **לא קיים** אצלה. לא מנוטרל — לא קיים.

---

## ה · הזמנות

**ה1 · הגיעה חלקית**
→ **שתי שורות**: חדשה עם מה שהגיע, והמקורית נשארת `ממתינה`.
→ למקורית אין יותר כפתור "הגיע" אלא הסבר.

**ה2 · פתיחת הזמנה**
לחצי על כרטיס הזמנה.
→ נפתח פאנל: מה הוזמן, תאריכים, לקוחה, וקישור לסחורה.
→ **אין בורר סטטוס** — הסטטוס נוצר מאירועים.

**ה3 · הזמנת לקוחה אצל העובדת**
כרטיס ספק ← הזמנות ← `הזמנה ללקוחה`.
→ הספק **מוצג ולא נבחר**. שם לקוחה **חובה**.

**ה4 · ההתראה על צירוף למשלוח**
פתחי הזמנת לקוחה אצל ספק שכבר יש לו הזמנה פתוחה.
→ בטופס עצמו מוצגות ההזמנות הקיימות **לפני** השמירה.
→ ואצל המנהלת נפתחת התראה `הזמנת לקוחה — אפשר לצרף למשלוח`.
✗ נפילה: ההתראה נפתחת גם על חידוש מלאי. היא נועדה ללקוחה בלבד.

---

## ו · אותה מערכת בשני תפקידים

**ו1 · שם האזור זהה**
→ אצל שתיהן "הזמנות וסחורה" / "מעקב הזמנות וסחורה". לא "תעודות משלוח".

**ו2 · אותו פאנל**
→ לחיצה על שורת סחורה אצל העובדת פותחת את **אותו** פאנל של המנהלת.

**ו3 · אותה שפת סטטוס**
→ אין `ממתין` / `בארכיון` באף מקום. רק שלבי הפייפליין.

**ו4 · רמזור**
→ בבורר הספקים: אדום = ממתין לאישור שלה · כתום = ממתין לחשבונית · ירוק = כלום.
✗ נפילה: הנקודה מציגה סכום או רומזת עליו.

---

## ז · מסמכים

**ז1 · מסמך של תעודה שנקלטה במייל נראה**
→ נפתח בצפייה. ⚠️ תעודות אלה **אינן בדרייב** — הקובץ ב‑Storage, וזה תקין.
✗ נפילה: "אין מסמך" למרות שהוא קיים.

**ז2 · מעבר לחשבונית מהפאנל**
→ `פתיחת החשבונית` נוחת על החשבונית עצמה.

---

## ח · תצוגה

**ח1 · כרטיסים ⇄ שורות**
→ אותו מתג ואותן שתי צורות כמו במסך הספקים, ונזכר בין ביקורים.

**ח2 · המסך הישן איננו**
→ אין יותר `מסמכים שהגיעו` / `קליטה ידנית` כמסך נפרד מתחת.

---

## ט · שער הכניסה לכרטסת

**ט1 · תנועה ממתינה נספרת ומסומנת**
→ הסכום **בתוך** היתרה, השורה מסומנת, ובאנר אומר שיש ממתינות.
✗ נפילה: היתרה קטנה מהאמת. יתרה שמחסירה חשבונית אמיתית היא הטעות הגרועה מבין השתיים.

**ט2 · אותה יתרה בארבעה מסכים**
רשימת ספקים · עמוד ספק · כרטסת · התאמת כרטסות.
→ **מספר זהה בכולם**, כולל ספק עם תנועה ממתינה.

---

## כ · נוסף אחרי הסבב הראשון (06.09.2026)

> כל מה שמכאן ואילך נבנה **אחרי** שהרשימה נכתבה, ורובו בעקבות סריקת קדם-פרודקשן
> או בקשה של בעלת העסק. **אלה הבדיקות הכי שוות עכשיו**, כי הן החדשות ביותר ולכן
> הפחות נבדקו.

**כ1 · הזמנה פותחת שורה — החוסם שתוקן** ⚠️ **קריטי**
פתחי הזמנה חדשה.
→ נוצרת שורה ברשימת הסחורה **ומופיע `delivery_note_id` על ההזמנה**.
✗ נפילה: ההזמנה נשמרת ואין שורה. זה בדיוק ה-23514 שהמיגרציה `20260906010000`
מתקנת — אם זה קורה, **המיגרציה לא הוחלה** ולא צריך לחפש באג בקוד.

**כ2 · כמה חשבוניות על שורה אחת** ⚠️ **קריטי**
הצמידי חשבונית, ואז **"הצמדת חשבונית נוספת"** והצמידי שנייה.
→ **שתיהן מופיעות** בפאנל, לכל אחת צפייה/פתיחה/ניתוק.
→ אישור של אחת בלבד **אינו** מעביר ל"בכרטסת". רק אחרי שתיהן.
✗ נפילה: השנייה נעלמת — אז המסך חזר לקרוא את העמודה הישנה.

**כ3 · חשבונית אחת על כמה שורות**
הצמידי אותה חשבונית לשתי תעודות ואשרי פעם אחת.
→ שתיהן ל"בכרטסת", **והיתרה זזה בסכום החשבונית פעם אחת**.

**כ4 · תעודה מהמייל אחרי קליטה ידנית**
קלטי סחורה ידנית, ואז שלחי תעודה מאותו ספק במייל.
→ בעמוד הסחורה של השורה הידנית: **קוביה "הגיעה תעודת משלוח מהספק"** עם צפייה.
→ "כן — מחליפה" ⇒ נשארת שורה אחת, עם המסמך של הספק.
→ "לא — משלוח אחר" ⇒ שתיהן נשארות, **והשאלה לא חוזרת**.
→ **הרמזור אדום** על הספק כל עוד לא נענתה.

**כ5 · דף בכתב יד**
טופס ← צילום ← אישור ← שמירה.
→ שלוש עמודות למילוי, **סה"כ מחושב לכל שורה**.
→ **המסמך נשמר** — לפתוח את התעודה שנוצרה ולראות אותו.
✗ נפילה: "אין מסמך". הצילום נזרק, וזה בדיוק מה שתוקן.

**כ6 · סכום שאינו סכום**
בהקלדה, שימי מחיר יחידה בן 12 ספרות ושמרי.
→ **התעודה נשמרת**, הסכום ריק.
✗ נפילה חמורה: התעודה לא נוצרת. שער הגלישה לא הגיע ל-`hadas-api`.

**כ7 · ביטול אישור**
אשרי חשבונית ואז בטלי.
→ תעודות חוזרות ל"ממתין לאישור" — **חוץ** מכאלה שהסחורה בהן לא הגיעה.

**כ8 · מי קלט/ה**
בקליטה ידנית, בחרי עובדת.
→ נשמר על השורה. ✗ נפילה: השדה נעלם מהטופס.

**כ9 · סנכרון בין מסכים** ⚠️ **הבדיקה של "מקור אמת יחיד"**
אשרי חשבונית במסך הסחורה, ואז **בלי לרענן** עברי לכרטיס הספק.
→ היתרה והסימון מעודכנים.
✗ נפילה: המספר ישן. אז ה-data bus לא מגיע לאותו hook.

**כ10 · הערת עובדת**
בתפקיד עובדת: פתחי חשבונית, כתבי הערה, שמרי.
→ נשמרת בלי לצאת מהמסך, **ומופיעה בפאנל הערות הספק** עם התג `חשבוניות`.

---

## ל · איפוס כרטסת וקבלה תמורת תשלום (07.09.2026)

**ל1 · האיפוס דורש סיבה** ⚠️ **קריטי**
בכרטיס ספק → כרטסת → "איפוס כרטסת" → "כן". נסי לשמור בלי לכתוב כלום.
→ הכפתור אפור ולא לוחיץ.
✗ נפילה: נשמר איפוס בלי סיבה. אז בעוד חצי שנה אין תשובה לשאלה היחידה שיישאלו עליו.

**ל2 · האיפוס מאפס** ⚠️ **קריטי**
בחרי ספק עם יתרה שאינה 0. אפסי עם הסיבה "בדיקה".
→ היתרה **בכותרת הכרטיס** נעשית 0, ובכרטסת מופיעה שורה דקה עם התאריך והסיבה.

**ל3 · הביטול מחזיר בדיוק** ⚠️ **קריטי — זו הרשת**
על שורת האיפוס, "בטל איפוס".
→ היתרה חוזרת **למספר שהיה לפני**, לשקל.
✗ נפילה: חזר מספר אחר. אז האיפוס לא היה שורה אלא שינוי — ואי אפשר לסמוך עליו.

**ל4 · שלושה מסכים, מספר אחד** ⚠️ **הבדיקה של "מקור אמת יחיד"**
אחרי איפוס, **בלי לרענן**, השוי את היתרה ב: רשימת הספקים · כרטיס הספק · מסך הכרטסת.
→ שלושתם מראים 0.
✗ נפילה: הרשימה מראה את המספר הישן. זו בדיוק התקלה של `spec/06-RULES.md §9`.

**ל5 · ההדפסה מתחילה אחרי האיפוס**
במסך הכרטסת, אחרי איפוס, לחצי הדפסה.
→ שורת האיפוס **לא מופיעה**, וגם התנועות שלפניה לא. הדוח מתחיל מהאיפוס והלאה.

**ל6 · הכרטסת מול הספק**
אחרי איפוס, שלחי כרטסת של אותו ספק לקליטה (או הריצי `reconcileStatement`).
→ ההשוואה נעשית מול היתרה **החדשה**.
✗ נפילה: נפתחה התראת `statement_mismatch` על ספק שאופס. אז הפונקציות לא קוראות את `ledger_resets`.

**ל7 · קבלה תמורת תשלום** ⚠️ **קריטי**
ספק שמשלמים לו תמורת קבלה: פתחי תעודה שממתינה לחשבונית → "התקבלה קבלה תמורת תשלום".
→ נפתחת רשימת התשלומים הפתוחים של הספק. סמני אחד → "סגור בקבלה".
→ התעודה עוברת ל"בכרטסת", והתשלום מסומן בכרטסת "נסגר בקבלה — לא נספר".
→ **היתרה מתיישרת** — התשלום כבר לא מושך אותה למינוס.

**ל8 · אין ניחוש**
אם לספק אין תשלומים פתוחים.
→ נאמר לך שצריך לרשום את התשלום קודם. **לא** נסגרת תעודה על תשלום שלא נבחר.

**ל9 · ביטול הקבלה**
"בטל סגירה בקבלה".
→ התעודה חוזרת ל"ממתין לחשבונית" והתשלום חוזר להיספר.

---

## מ · כפילויות ופענוח הטבלה (07.09.2026)

**מ1 · תעודה כפולה נתפסת** ⚠️ **קריטי**
אם יש לך היום שתי שורות על אותה תעודה — הן נוצרו לפני התיקון ולא ייתפסו רטרואקטיבית.
לבדיקה: העבירי לתיבה **את אותה תעודת משלוח פעם שנייה, במייל נפרד**.
→ נפתחת התראה אדומה **"תעודת משלוח כפולה"**.
→ השורה השנייה מסומנת ברשימת הסחורה: **"תעודה כפולה — יש לפרק את השגויה"**.
✗ נפילה: נוצרה שורה שנייה בלי סימון ובלי התראה. אז הבדיקה עדיין רק לפי מזהה המייל.

**מ2 · הכפילות לא נמחקת**
→ **שתי** השורות קיימות. המערכת לא בוחרת איזו נכונה.
זה בכוונה: איזו מהן המשלוח האמיתי היא שאלה על סחורה, לא על נתונים. הפירוק נעשה ידנית מתוך המסך.

**מ3 · הטבלה עולה מהתעודה** ⚠️ **קריטי**
המתיני לתעודת משלוח **חדשה** שנקלטת מהמייל (ישנות לא ינותחו מחדש), ופתחי אותה.
→ באזור "מה הגיע" מופיעה **טבלה**: פריט · כמות · מחיר ליחידה · סה"כ.
✗ נפילה: עדיין רשימת שמות בלי מספרים. אז הפרומפט לא נפרס.

**מ4 · הסכום מחושב, לא מומצא**
→ שורת "סה"כ לפי הפריטים" מופיעה **רק** אם לכל הפריטים יש מחיר.
אם לפריט אחד אין מחיר — אין סכום כולל. סכום חלקי נראה כמו שווי המשלוח ואינו.

**מ5 · תעודות ישנות לא נשברות**
פתחי תעודה ישנה שכבר יש בה רשימת פריטים.
→ הרשימה מוצגת כטבלה עם עמודת "פריט" בלבד, בלי עמודות ריקות. שום שורה לא נעלמת.

**מ6 · צילום תעודה שכבר הגיעה במייל** ⚠️ **קריטי**
בחרי תעודה שנקלטה מהמייל, וצלמי את אותה תעודה מתוך הספק (דלת "צילום").
→ **לא נוצרת שורה שנייה.** מופיע "המסמך כבר קיים במערכת · תעודה N כבר נקלטה מהמייל",
ואצל המנהלת גם כפתור **"פתחי את דף הסחורה"** שמוביל לשורה הקיימת.
✗ נפילה: נוצרה שורה שנייה. אז דגל `captureMode` לא הגיע לפונקציה.

**מ7 · צילום תעודה חדשה עדיין עובד**
צלמי תעודה שאין לה מקבילה במערכת.
→ נקלטת רגיל, נוצרת שורה. הבדיקה תופסת **רק** מספר תעודה שכבר קיים לאותו ספק.

---

## נ · מקור התעודה, הכפילות והפייפליין של החשבונית (08.09.2026)

**נ1 · לכל שורת סחורה יש מקור** ⚠️ **קריטי**
פתחי את מעקב הסחורה (טבלה **וגם** כרטיסים).
→ מתחת לשם הספק כתוב מאיפה השורה: `מסמך מהספק · מייל` · `צילום מסמך` ·
  `צילום דף בכתב יד` · `הקלדה` · `הזמנה` · `נפתח מחשבונית`.
✗ נפילה: תעודה שצולמה כתוב עליה "מייל". אז המיגרציה `20260908010000` לא הוחלה —
  זה מילוי אחורה, לא באג בקוד.

**נ2 · "אין מסמך" אומר למה** ⚠️ **זו ההערה שפתחה את הסבב**
פתחי שורה שנקלטה **בהקלדה**, ואז שורה שנקלטה מ**דף בכתב יד**.
→ בהקלדה: "הפריטים הוקלדו ידנית — לא היה מסמך מקור". זו עובדה, בלי אזהרה.
→ בדף בכתב יד בלי קובץ: "הדף צולם ונקרא אך הצילום לא נשמר", **ובכתום**:
  "הפענוח נשמר בלי המסמך שממנו נקרא".
✗ נפילה: שתיהן נראות אותו דבר. אז אי אפשר להבדיל בין קבלה תקינה לבין מסמך שאבד.

**נ3 · הדף בכתב יד מזהיר לפני השמירה**
צלמי דף כשההעלאה נכשלת (או בדקי בשורה שנוצרה).
→ **לפני** כפתור השמירה מופיע: "הצילום לא נשמר — התעודה תישמר בלי המסמך".

**נ4 · הפופאפ לא פותח את הכפילות שהוא מונע** ⚠️ **קריטי**
צרי הזמנה לספק, ואז קליטת סחורה ← הקלדה **לאותו ספק**.
→ עולה "כבר יש תעודה ממתינה". ✗ נפילה: לא עולה כלום והחלון נסגר —
  אז התשובה נבלעת שוב ושום שורה לא נכתבה.
→ "זו היא" ⇒ **שורה אחת**, של ההזמנה, עם הפריטים שהוקלדו.
→ "לא, זו סחורה אחרת" ⇒ שתי שורות בדיוק, **לא שלוש**. שורת ההזמנה זזה, לא נשארת
  ריקה מאחור.

**נ5 · אימוץ לא דורס את הספק** ⚠️ **קריטי**
לספק שיש לו תעודה שהגיעה במייל (עם פריטים, סכום ומסמך): קלטי סחורה ידנית ובחרי
"זו היא".
→ בשורה נשארים **הפריטים של הספק, הסכום שלו והמסמך שלו**, והמקור נשאר
  `מסמך מהספק · מייל`.
✗ נפילה חמורה: הפריטים הוחלפו במה שהוקלד. אז השורה שמרה מספר ואיבדה ראיה.

**נ6 · הכפתור בחשבונית מציג את מה שהוא פתח** ⚠️ **קריטי**
פתחי חשבונית לא מקושרת ← "ממתינה לסחורה".
→ מיד מופיע השלב `חשבונית ללא סחורה` וקישור **"פתיחת הפייפליין"** שנוחת על
  שורת הסחורה.
✗ נפילה: נראה שלא קרה כלום. זה היה הבאג — השורה נפתחה בכל לחיצה ולא הוצגה,
  אז נוצרו שורות מיותרות בכל לחיצה חוזרת.

**נ7 · חשבונית פותחת פייפליין לבד — ורק לספק סחורה**
המתיני לחשבונית **חדשה** במייל מספק שיש לו היסטוריית תעודות/הזמנות.
→ נפתחה שורה ב`חשבונית ללא סחורה` בלי שנגעת.
→ מספק שירות (רו"ח, שכירות) — **לא נפתחת שורה**. זו ההחלטה, לא תקלה.
→ אם לאותו ספק כבר יש תעודה שממתינה לחשבונית — **לא נפתח שלד**; החשבונית מוצעת
  כמועמדת להצמדה באותה תעודה, וזה נשאר החלטה של אדם.

**נ8 · העתקת הערה**
כתבי הערה בחשבונית ושמרי. לחצי על אייקון ההעתקה שליד הכותרת "הערות לחשבונית".
→ האייקון הופך ל-✓ **"הועתק"** לרגע, והדבקה במייל נותנת את **טקסט ההערה בלבד**.
→ אותו דבר במסך התאמת הכרטסות, ובפאנל הערות הספק על כל שורה — גם על הנאספות.
✗ נפילה: אומר "הועתק" וההדבקה ריקה. אז ה-fallback לא רץ ואסור לסמוך על הכפתור.
→ על הערה ריקה **אין** אייקון בכלל. זה בכוונה.

**נ9 · רישום סחורה מתוך השורה** ⚠️ **קריטי**
פתחי שורה ב`חשבונית ללא סחורה` (חשבונית שהגיעה לפני הסחורה).
→ בראש העמוד שורה **מקופלת**: `▾ הסחורה הגיעה? · הקלדה · דף בכתב יד · צילום התעודה`.
  לחיצה עליה פותחת את שלוש הדלתות; "סגירה" מתחתן מחזירה אותה למצב סגור.
→ הקלדה ← פריטים ← **"שמירה לשורה הזו"** ⇒ **אותה שורה** מתעדכנת ועוברת
  ל`ממתין לאישור`, והבלוק נסגר לבד. ✗ נפילה חמורה: נפתחה שורה שנייה.
→ על שלד של **הזמנה** (בלי חשבונית) אותה פעולה מעבירה ל`ממתין לחשבונית` ולא
  ל"ממתין לאישור" — אין מול מה לאשר.
→ **בלי ספק או בלי אף פריט** הכפתור אפור, ולידו כתוב מה חסר. ✗ נפילה: אפור ושותק —
  זה בדיוק מה שנראה כמו מערכת שבורה.

**נ10 · צילום תעודה לתוך שורה קיימת** ⚠️ **קריטי — הרצף הנפוץ בחנות**
לספק שהחשבונית שלו נקלטה מהמייל ופתחה שורה: פתחי את השורה ← **צילום תעודת המשלוח**
← צלמי את התעודה שהגיעה עם הסחורה.
→ "התעודה נצמדה לשורה", **ולא נוצרת שורה שנייה**.
→ בשורה מופיעים מספר התעודה, הפריטים והמסמך עצמו (אפשר לפתוח בצפייה).
→ אם על התעודה כתוב ספק אחר, או אם המספר כבר קיים בשורה אחרת — נאמר לך בכתום.
  ההצמדה בכל זאת מתבצעת, כי את בחרת את השורה.
✗ נפילה: נקלטה שורה חדשה. אז `deliveryNoteId` לא הגיע לקליטה, כלומר
  `invoices-ingest` לא נפרסה.

---

## י · מה שטרם נבנה — לא לבדוק

- התראות מבוססות‑זמן (7 ימים · חשבונית מרכזת · ספק להשלמה)
- החלפת מסמך **שכבר קיים** בתעודה — צילום שדורס מסמך קודם. מה שכן נבנה הוא
  **הצמדה לשורה שאין בה מסמך** (נ10); דריסה של מסמך קיים עדיין לא, במכוון.

**שתי שורות ירדו מכאן (08.09), כי הן כבר קיימות ונבדקות למעלה:**
`דף בכתב יד (תבנית + צילום)` — נבדק בכ5 ובנ3 — ו`צילום לתוך תעודה קיימת`, שנבנה
היום ויושב בנ10. רשימת "לא לבדוק" שמכילה פיצ'רים קיימים גרועה מרשימה ריקה: היא
שולחת את הבודקת לדלג בדיוק על מה שהכי חדש, ולכן הכי פחות נבדק.
